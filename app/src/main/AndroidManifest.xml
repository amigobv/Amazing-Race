<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="moc5.amazingrace" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="ANDROID.PERMISSION.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="ANDROID.PERMISSION.ACCESS_FINE_LOCATION" />

    <application
        android:name=".AmazingRace"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AmazingRaceMainActivity"
            android:label="@string/app_name" >
        </activity>

        <!-- <activity android:name=".RouteListActivity" /> -->
        <activity
            android:name=".RouteDetailsActivity"
            android:label="@string/RouteDetailsLabel" />
        <activity
            android:name=".NextCheckpointActivity"
            android:label="@string/nextCheckpoint" />
        <activity
            android:name=".PreviousCheckpointsActivity"
            android:label="@string/PreviousCheckpoint" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".ViewCheckpoint" >
        </activity>
    </application>

</manifest>
